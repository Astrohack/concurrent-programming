<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour" Height="auto" Width="auto">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- UI kontrolki -->
            <RowDefinition Height="*"/>
            <!-- Canvas -->
        </Grid.RowDefinitions>

        <!-- Panel z +, -, TextBlock i Generate Balls -->
        <Grid Grid.Row="1" Height="40" Width="400" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="-" Width="40" x:Name="DecrementBallsBtn" Click="Decrement_Click" Height="40"/>
            <TextBlock x:Name="BallsQuantityTextBlock" Grid.Column="1" Text="1" 
                       FontSize="16" VerticalAlignment="Center" 
                       HorizontalAlignment="Center" Margin="10"/>
            <Button Grid.Column="2" Content="+" Width="40" x:Name="IncrementBallsBtn" Click="Increment_Click" Height="40"/>
            <Button Grid.Column="3" Content="Generate Balls" x:Name="GenerateBallsBtn" Click="GenerateBalls_Click"/>
        </Grid>

        <!-- Border z Canvasem -->
        <Border x:Name="TableBorder" BorderThickness="5" BorderBrush="Green">
            <ItemsControl x:Name="Canvas" ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width= "{Binding Diameter}" Height="{Binding Diameter}" Fill="Blue" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </Grid>
</Window>